#include <bits/stdc++.h>

using namespace std;
#define ll long long
const int N = 1e5 + 5;

int main()
{

  int t;
  cin >> t;

  while (t--)
  {

    ll n, x, y; cin >> n;

    vector<ll>  arr(n);

    vector<ll> brr(n);

    for (ll i = 0; i < n; i++)
    {
      
      cin>>arr[i];
    }

    for (ll i = 0; i < n; i++)
    {
      
      cin>>brr[i];
    }

    for (int i = 0; i < n; i++)
    {
      /* code */
      for (int j = 0; j < count; j++)
      {
        /* code */
      }
      
    }
    
    
    

   
  }
  return 0;
}
