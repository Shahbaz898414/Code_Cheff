#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
 
 
 
int main()
{
    
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
	ll t;
	cin>>t;
	// set<ll>::iterator it;
	while(t--) {
    ll n,m;cin>>n>>m;
    vector<ll> arr(n);

    for (ll i = 0; i < n; i++) {
      cin>>arr[i];
    }

    ll l,r,k;cin>>l>>r>>k;
          
  }
    return 0;
}