#include <bits/stdc++.h>
using namespace std;

#define ll long long

bool checkChef(char c, char h, char e, char f)
{
  if ((c == 'C' || c == '?') && (h == 'H' || h == '?') && (e == 'E' || e == '?') && (f == 'F' || f == '?'))
    return true;
  return false;
}

int main()
{
  int T = 1;
  cin >> T;
  while (T--)
  {
    ll a,b,n;cin>>a>>b>>n;
    
  }

  return 0;
}
